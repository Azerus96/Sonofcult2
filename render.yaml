services:
  - type: web
    name: chinese-poker
    env: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: AI_PROGRESS_TOKEN
        sync: false
    buildCommand: docker build -t chinese-poker .
    startCommand: docker run -p $PORT:$PORT chinese-poker
